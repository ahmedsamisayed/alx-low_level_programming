#include "main.h"
#include <stdlib.h>
/**
 * add - add two numbers
 *@a: the first number
 *@b: the second number
 *
 * Return: result = 1 if letter is lower, else 0
 */
int add(int a, int b)
{
int x = a + b;
return (x);
}
