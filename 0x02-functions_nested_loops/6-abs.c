#include "main.h"
#include <stdlib.h>
/**
 * _abs - get the absolute value
 *@a: number to get absolute value to
 *
 * Return: result = 1 if letter is lower, else 0
 */
int _abs(int a)
{
int x = abs(a);
return (x);
}
